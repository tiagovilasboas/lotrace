# Design system (vendored)

Mirror of [lotrace-design-system](https://github.com/tiagovilasboas/lotrace-design-system).

- **Tokens:** `tokens/` — imported by `frontend/src/index.css`
- **Assets:** sync from upstream `assets/` (brand, pieces, car seats)

To refresh: download ZIP or copy from the DS repo; keep `IsoIcons.tsx` paths aligned with `assets/pieces/*.svg`.
